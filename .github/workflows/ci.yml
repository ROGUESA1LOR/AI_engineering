name: Production Integration Test Suite
on: [push]

jobs:
  run-tests:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v4

      - name: Execute Core Logic Check
        run: |
          echo "Simulating production system launch..."
          # A deliberate intentional error: this file doesn't exist yet!
          python3 broken_script.py
